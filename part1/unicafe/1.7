import { useState } from 'react'

const Button = (props) => (
  <button onClick={props.handleClick}>{props.name}</button>
)

const App = () => {
  // save clicks of each button to its own state
  const [good, setGood] = useState(0)
  const [neutral, setNeutral] = useState(0)
  const [bad, setBad] = useState(0)

  const increaseToValue = (props, name) => {
    if (name === 'good') {
      setGood(props + 1)
      console.log('good:', good, 'neutral:', neutral, 'bad:', bad)
    } else if (name === 'neutral') {
        setNeutral(props + 1)
        console.log('good:', good, 'neutral:', neutral, 'bad:', bad)
    } else {
        setBad(props + 1)
        console.log('good:', good, 'neutral:', neutral, 'bad:', bad)
      }
    }

  return (
    <div>
      <h1>give feedback</h1>
      <Button handleClick={() => increaseToValue(good, 'good')} name="good" />
      <Button handleClick={() => increaseToValue(neutral, 'neutral')} name="neutral" />
      <Button handleClick={() => increaseToValue(bad, 'bad')} name="bad" />
      <h1>statistics</h1>
      <p>
        good {good} <br />
        neutral {neutral} <br />
        bad {bad} <br />
        all {good + neutral + bad} <br />
        average { (good + neutral + bad) === 0 ? "-" : ((good - bad) / (good + neutral + bad)) }<br />
        positive { (good + neutral + bad) === 0 ? "-" : ((good) / (good + neutral + bad) * 100) } % <br />
      </p>
    </div>
  )
}

export default App
